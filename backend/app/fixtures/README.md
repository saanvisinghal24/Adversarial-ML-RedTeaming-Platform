# Contract fixtures

Frozen examples of ARCHITECTURE.md contracts #3–#6. They exist so M2/M3/M4 can develop
against a known-good payload without running the API, and so the contract-shape test has
something stable to assert against.

If ARCHITECTURE.md changes, change these in the same commit.
